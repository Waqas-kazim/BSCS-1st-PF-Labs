#include<iostream>
using namespace std;
main()
{
    string subType;
    cout << "enter subscription type: " ;
    cin >> subType;
    if(subType=="premium")
    cout<<"Access to all features granted";
}