#include<iostream>
using namespace std;
main()
{
    float length, width;
    cout << "enter width of rectangle: ";
    cin >> width;
    cout << "enter length of rectangle: ";
    cin >> length;
    if(length == width){
        cout<<"This is square";
    }
}